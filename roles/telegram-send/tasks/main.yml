---
- name: "tgsend:configure"
  ansible.builtin.template:
    src: "telegram-send.conf.j2"
    dest: "/etc/telegram-send.conf"
    owner: "root"
    group: "root"
    mode: "0640"
  when:
    - "tgsend_bot_token is defined"
    - "tgsend_chat_id is defined"
  notify: "tgsend|test"
